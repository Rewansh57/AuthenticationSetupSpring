server:
  port:
    9000
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/MyProject
    username: postgres
    password: rewansh@12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update  # Use "create", "create-drop", "validate", or "none" as needed
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
